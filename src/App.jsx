mport { useState, useRef, useEffect } from "react";const SUPA_URL = "https://yxcoknkmmxdyhkkolead.supabase.co";const SUPA_KEY = "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Inl4Y29rbmttbXhkeWhra29sZWFkIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzgzNjA3MjgsImV4cCI6MjA5MzkzNjcyOH0.9L4QDVdzBr7lqsxNZYYEKsrPERvKnlH-8rvptdKjy6s";async function _query(method, table, opts = {}) {  try {    let url = `${SUPA_URL}/rest/v1/${table}?select=${opts.select || "*"}`;    if (opts.filters) opts.filters.forEach(f => { url += `&${f}`; });    if (opts.order) url += `&order=${opts.order}.${opts.ascending === false ? "desc" : "asc"}`;    const res = await fetch(url, { headers: { apikey: SUPA_KEY, Authorization: `Bearer ${SUPA_KEY}`, "Content-Type": "application/json" } });    const data = await res.json();    return { data: Array.isArray(data) ? data : [], error: res.ok ? null : data };  } catch (e) { return { data: [], error: e }; }}async function _mutate(method, table, body, filter = "") {  try {    let url = `${SUPA_URL}/rest/v1/${table}${filter ? `?${filter}` : ""}`;    const res = await fetch(url, { method, headers: { apikey: SUPA_KEY, Authorization: `Bearer ${SUPA_KEY}`, "Content-Type": "application/json", Prefer: "return=representation" }, body: body ? JSON.stringify(body) : undefined });    return { data: res.ok ? await res.json().catch(() => null) : null, error: res.ok ? null : "error" };  } catch (e) { return { data: null, error: e }; }}const db = {  from: (t) => ({    select: (c = "*") => ({      eq: (col, val) => ({ order: (o, opts) => _query("GET", t, { select: c, filters: [`${col}=eq.${val}`], order: o, ascending: opts?.ascending }), then: (cb) => _query("GET", t, { select: c, filters: [`${col}=eq.${val}`] }).then(cb) }),      order: (o, opts) => _query("GET", t, { select: c, order: o, ascending: opts?.ascending }),      then: (cb) => _query("GET", t, { select: c }).then(cb),    }),    insert: (b) => _mutate("POST", t, b),    update: (b) => ({ eq: (col, val) => _mutate("PATCH", t, b, `${col}=eq.${val}`) }),    delete: () => ({ eq: (col, val) => _mutate("DELETE", t, null, `${col}=eq.${val}`) }),  }),};const C = {  vert: "#1B5E20", vertL: "#E8F5E9", or: "#F9A825", orL: "#FFF8E1",  blanc: "#FFFFFF", gris: "#F5F5F5", dark: "#424242", texte: "#1A1A1A",  rouge: "#C62828", bleu: "#1565C0", orange: "#E65100",};const fmt = (n) => Number(n).toLocaleString("fr-FR") + " GNF";const genCode = () => "#HL-" + Math.floor(100 + Math.random() * 900);const isNew = (d) => d && (Date.now() - new Date(d).getTime()) < 48 * 3600 * 1000;const STATUTS = [  { key: "en_attente", label: "Commande re莽ue", icon: "馃摜", color: "#F57F17" },  { key: "confirmee", label: "Confirm茅e", icon: "鉁�", color: C.vert },  { key: "en_route", label: "En route", icon: "馃殮", color: C.bleu },  { key: "livree", label: "Livr茅e", icon: "馃帀", color: C.vert },];const CATS = ["Tout", "T茅l茅phones", "Mode", "脡lectronique", "V茅hicules", "Mobilier", "Alimentation", "Autre"];const CAT_ICONS = { "Tout": "馃彧", "T茅l茅phones": "馃摫", "Mode": "馃憲", "脡lectronique": "鈿�", "V茅hicules": "馃浀", "Mobilier": "馃泲锔�", "Alimentation": "馃尵", "Autre": "馃摝" };const PRODUITS_DEMO = [  { id: 1, nom: "iPhone 14 Pro Max", prix: 8500000, ville: "Conakry", categorie: "T茅l茅phones", etat: "Tr猫s bon 茅tat", description: "iPhone 14 Pro Max 256Go Noir Sid茅ral. Batterie 97%, bo卯te originale.", vendeur_nom: "Mamadou Diallo", vendeur_tel: "622001122", code: "#HL-101", statut: "approuve", image_url: null, note_moy: 4.5, nb_notes: 12, created_at: new Date(Date.now() - 10000).toISOString() },  { id: 2, nom: "Robe Bazin Brod茅e", prix: 450000, ville: "Lab茅", categorie: "Mode", etat: "Neuf", description: "Robe bazin brod茅e taille M/L. Couture guin茅enne traditionnelle.", vendeur_nom: "Fatoumata Bah", vendeur_tel: "622112233", code: "#HL-102", statut: "approuve", image_url: null, note_moy: 5, nb_notes: 8, created_at: new Date(Date.now() - 20000).toISOString() },  { id: 3, nom: "T茅l茅vision Samsung 55\"", prix: 3200000, ville: "Conakry", categorie: "脡lectronique", etat: "Bon 茅tat", description: "TV Samsung 55\" 4K Smart TV. T茅l茅commande et c芒bles inclus.", vendeur_nom: "Ibrahima Sow", vendeur_tel: "624001122", code: "#HL-103", statut: "approuve", image_url: null, note_moy: 4, nb_notes: 5, created_at: new Date(Date.now() - 86400000).toISOString() },  { id: 4, nom: "Moto Tricycle Cargo", prix: 12000000, ville: "Lab茅", categorie: "V茅hicules", etat: "Bon 茅tat", description: "Tricycle cargo 200cc, faible kilom茅trage.", vendeur_nom: "Alpha Barry", vendeur_tel: "628001122", code: "#HL-104", statut: "approuve", image_url: null, note_moy: 4.5, nb_notes: 3, created_at: new Date(Date.now() - 172800000).toISOString() },  { id: 5, nom: "Canap茅 3 Places", prix: 1800000, ville: "Conakry", categorie: "Mobilier", etat: "Tr猫s bon 茅tat", description: "Canap茅 tissu beige 3 places.", vendeur_nom: "Aissatou Camara", vendeur_tel: "622551122", code: "#HL-105", statut: "approuve", image_url: null, note_moy: 3.5, nb_notes: 6, created_at: new Date(Date.now() - 300000).toISOString() },  { id: 6, nom: "G茅n茅rateur 5KVA", prix: 6500000, ville: "Lab茅", categorie: "脡lectronique", etat: "Bon 茅tat", description: "Groupe 茅lectrog猫ne 5KVA essence.", vendeur_nom: "Oumar Kouyat茅", vendeur_tel: "622661122", code: "#HL-106", statut: "approuve", image_url: null, note_moy: 4, nb_notes: 9, created_at: new Date(Date.now() - 3600000).toISOString() },];const DEMO_USERS = [  { email: "admin@halimad.com", password: "admin123", nom: "Admin HALIMAD", role: "admin", ville: "Conakry" },  { email: "vendeur@halimad.com", password: "vendeur123", nom: "Mamadou Diallo", role: "vendeur", ville: "Conakry", telephone: "622001122" },  { email: "acheteur@halimad.com", password: "acheteur123", nom: "Fatoumata Bah", role: "acheteur", ville: "Conakry", telephone: "628112233" },];// 鈹€鈹€鈹€ Styles 鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€const S = {  app: { fontFamily: "'Segoe UI', Tahoma, Geneva, Verdana, sans-serif", minHeight: "100vh", backgroundColor: C.gris, color: C.texte },  nav: { backgroundColor: C.vert, padding: "0 14px", display: "flex", alignItems: "center", justifyContent: "space-between", height: "56px", boxShadow: "0 2px 8px rgba(0,0,0,0.3)", position: "sticky", top: 0, zIndex: 200 },  logo: { color: C.or, fontSize: "20px", fontWeight: "900", letterSpacing: "2px", cursor: "pointer", flexShrink: 0 },  navSub: { color: "rgba(255,255,255,0.6)", fontSize: "9px", marginTop: "-3px" },  navBtn: (g) => ({ backgroundColor: g ? C.or : "rgba(255,255,255,0.15)", color: g ? C.vert : "#fff", border: "none", borderRadius: "8px", padding: "6px 12px", fontWeight: "700", cursor: "pointer", fontSize: "11px" }),  hero: { background: `linear-gradient(135deg, ${C.vert} 0%, #2E7D32 60%, #1B5E20 100%)`, padding: "36px 14px 24px", textAlign: "center" },  heroTitle: { color: C.or, fontSize: "32px", fontWeight: "900", margin: "0 0 4px", letterSpacing: "3px" },  heroSub: { color: "rgba(255,255,255,0.85)", fontSize: "12px", marginBottom: "16px" },  wrap: { maxWidth: "1100px", margin: "0 auto", padding: "14px 10px" },  catBar: { display: "flex", gap: "7px", overflowX: "auto", paddingBottom: "6px", marginBottom: "14px", scrollbarWidth: "none" },  catChip: (a) => ({ backgroundColor: a ? C.vert : C.blanc, color: a ? C.blanc : C.dark, border: `1.5px solid ${a ? C.vert : "#E0E0E0"}`, borderRadius: "20px", padding: "5px 11px", fontWeight: "700", cursor: "pointer", fontSize: "11px", whiteSpace: "nowrap", flexShrink: 0 }),  grid: { display: "grid", gridTemplateColumns: "repeat(auto-fill, minmax(200px, 1fr))", gap: "12px" },  card: { backgroundColor: C.blanc, borderRadius: "14px", overflow: "hidden", boxShadow: "0 2px 8px rgba(0,0,0,0.07)", cursor: "pointer", transition: "transform 0.2s, box-shadow 0.2s", position: "relative" },  input: { width: "100%", padding: "10px 12px", borderRadius: "10px", border: "1.5px solid #E0E0E0", fontSize: "14px", marginBottom: "10px", outline: "none", boxSizing: "border-box", fontFamily: "inherit" },  label: { fontSize: "12px", fontWeight: "700", color: C.dark, marginBottom: "4px", display: "block" },  sel: { width: "100%", padding: "10px 12px", borderRadius: "10px", border: "1.5px solid #E0E0E0", fontSize: "14px", marginBottom: "10px", outline: "none", boxSizing: "border-box", backgroundColor: C.blanc, fontFamily: "inherit" },  btn: { backgroundColor: C.vert, color: C.blanc, border: "none", borderRadius: "10px", padding: "12px", width: "100%", fontWeight: "800", fontSize: "14px", cursor: "pointer" },  waBtn: { backgroundColor: "#25D366", color: "#fff", border: "none", borderRadius: "8px", padding: "8px", width: "100%", fontWeight: "700", cursor: "pointer", fontSize: "12px", marginTop: "7px", display: "flex", alignItems: "center", justifyContent: "center", gap: "5px" },  badge: (c, bg) => ({ backgroundColor: bg || c + "18", color: c, borderRadius: "6px", padding: "2px 7px", fontSize: "10px", fontWeight: "700", display: "inline-block" }),  row: { backgroundColor: C.blanc, borderRadius: "12px", padding: "12px", marginBottom: "8px", boxShadow: "0 1px 5px rgba(0,0,0,0.05)" },  dashHead: { background: `linear-gradient(135deg, ${C.vert}, #2E7D32)`, padding: "18px", color: C.blanc, borderRadius: "0 0 16px 16px", marginBottom: "16px" },  statGrid: { display: "grid", gridTemplateColumns: "repeat(auto-fill, minmax(110px, 1fr))", gap: "9px", marginBottom: "16px" },  statCard: { backgroundColor: C.blanc, borderRadius: "12px", padding: "13px", textAlign: "center", boxShadow: "0 2px 6px rgba(0,0,0,0.05)" },  tab: (a) => ({ padding: "7px 14px", borderRadius: "8px", border: "none", backgroundColor: a ? C.vert : "transparent", color: a ? C.blanc : C.dark, fontWeight: "700", cursor: "pointer", fontSize: "11px" }),  authCard: { backgroundColor: C.blanc, borderRadius: "18px", padding: "24px 20px", maxWidth: "380px", margin: "24px auto", boxShadow: "0 8px 28px rgba(0,0,0,0.1)" },  modal: { position: "fixed", top: 0, left: 0, right: 0, bottom: 0, backgroundColor: "rgba(0,0,0,0.55)", zIndex: 500, display: "flex", alignItems: "flex-end", justifyContent: "center" },  modalBox: { backgroundColor: C.blanc, borderRadius: "20px 20px 0 0", padding: "22px 18px", width: "100%", maxWidth: "500px", maxHeight: "90vh", overflowY: "auto" },  footer: { backgroundColor: C.vert, color: "rgba(255,255,255,0.75)", textAlign: "center", padding: "20px", marginTop: "36px", fontSize: "11px" },};// 鈹€鈹€鈹€ Utils 鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€function useToast() {  const [toasts, setToasts] = useState([]);  const add = (msg, type = "info", icon = "鈩癸笍") => {    const id = Date.now();    setToasts(p => [...p, { id, msg, type, icon }]);    setTimeout(() => setToasts(p => p.filter(t => t.id !== id)), 3500);  };  return { toasts, add };}function Toast({ toasts }) {  return (    <div style={{ position: "fixed", bottom: "80px", left: "50%", transform: "translateX(-50%)", zIndex: 9999, display: "flex", flexDirection: "column", gap: "6px", alignItems: "center", pointerEvents: "none" }}>      {toasts.map(t => (        <div key={t.id} style={{ backgroundColor: t.type === "success" ? C.vert : t.type === "error" ? C.rouge : "#333", color: "#fff", borderRadius: "30px", padding: "9px 18px", fontSize: "12px", fontWeight: "700", boxShadow: "0 4px 16px rgba(0,0,0,0.25)", display: "flex", alignItems: "center", gap: "6px", whiteSpace: "nowrap" }}>          {t.icon} {t.msg}        </div>      ))}    </div>  );}function Skeleton() {  return (    <div style={{ ...S.card, overflow: "hidden" }}>      <div style={{ height: "160px", background: "linear-gradient(90deg,#f0f0f0 25%,#e8e8e8 50%,#f0f0f0 75%)", backgroundSize: "200% 100%", animation: "shimmer 1.4s infinite" }} />      <div style={{ padding: "10px" }}>        {[["80%","13px","7px"],["50%","16px","5px"],["60%","10px","0"]].map(([w,h,mb],i) => (          <div key={i} style={{ width:w, height:h, marginBottom:mb, borderRadius:"5px", background:"linear-gradient(90deg,#f0f0f0 25%,#e8e8e8 50%,#f0f0f0 75%)", backgroundSize:"200% 100%", animation:"shimmer 1.4s infinite" }} />        ))}      </div>      <style>{`@keyframes shimmer{0%{background-position:-200% 0}100%{background-position:200% 0}}`}</style>    </div>  );}// 鈹€鈹€鈹€ 脡toiles 鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€function Stars({ note, size = 14, interactive = false, onRate }) {  const [hov, setHov] = useState(0);  return (    <div style={{ display: "flex", gap: "2px" }}>      {[1,2,3,4,5].map(i => (        <span key={i}          style={{ fontSize: size, cursor: interactive ? "pointer" : "default", color: i <= (hov || note) ? C.or : "#ddd", transition: "color 0.1s" }}          onMouseEnter={() => interactive && setHov(i)}          onMouseLeave={() => interactive && setHov(0)}          onClick={() => interactive && onRate && onRate(i)}        >鈽�</span>      ))}    </div>  );}// 鈹€鈹€鈹€ Upload Zone 鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€function UploadZone({ label, files, setFiles, maxFiles = 5, type = "photo" }) {  const inputId = useRef("inp_" + Math.random().toString(36).slice(2)).current;  const processFiles = (rawFiles) => {    const arr = Array.from(rawFiles).filter(f => type === "photo" ? f.type.startsWith("image/") : f.type.startsWith("video/")).slice(0, maxFiles - files.length);    if (!arr.length) return;    Promise.all(arr.map(file => new Promise(resolve => {      const reader = new FileReader();      reader.onload = (ev) => {        if (file.type.startsWith("image/")) {          const imgEl = new Image();          imgEl.onload = () => {            const canvas = document.createElement("canvas");            const MAX = 800; let w = imgEl.width, h = imgEl.height;            if (w > MAX) { h = Math.round(h * MAX / w); w = MAX; }            canvas.width = w; canvas.height = h;            canvas.getContext("2d").drawImage(imgEl, 0, 0, w, h);            resolve({ name: file.name, preview: canvas.toDataURL("image/jpeg", 0.8), type: "image", size: Math.round(file.size / 1024) });          };          imgEl.src = ev.target.result;        } else resolve({ name: file.name, preview: ev.target.result, type: "video", size: Math.round(file.size / 1024) });      };      reader.readAsDataURL(file);    }))).then(res => setFiles(prev => [...prev, ...res].slice(0, maxFiles)));  };  return (    <div style={{ marginBottom: "12px" }}>      <div style={{ fontSize: "12px", fontWeight: "700", color: C.dark, marginBottom: "5px" }}>{label}</div>      <label htmlFor={inputId} style={{ display: "flex", flexDirection: "column", alignItems: "center", border: `2px dashed ${C.vert}`, borderRadius: "10px", padding: "16px", backgroundColor: C.vertL, cursor: "pointer", marginBottom: "8px", textAlign: "center", userSelect: "none" }}>        <span style={{ fontSize: "26px", marginBottom: "4px" }}>{type === "photo" ? "馃摲" : "馃帴"}</span>        <span style={{ fontWeight: "800", color: C.vert, fontSize: "13px" }}>{files.length > 0 ? `鉁� ${files.length} fichier(s) 鈥� Ajouter d'autres` : `Appuyez pour choisir ${type === "photo" ? "des photos" : "une vid茅o"}`}</span>        <span style={{ fontSize: "10px", color: "#888", marginTop: "3px" }}>{type === "photo" ? "JPG, PNG 路 Max 5 路 Auto-compress茅" : "MP4, MOV 路 Max 1"}</span>      </label>      <input id={inputId} type="file" accept={type === "photo" ? "image/*" : "video/*"} multiple={type === "photo"} style={{ position: "absolute", opacity: 0, width: "1px", height: "1px" }} onChange={e => { if (e.target.files?.length) processFiles(e.target.files); e.target.value = ""; }} />      {files.length > 0 && (        <div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fill, minmax(72px, 1fr))", gap: "7px" }}>          {files.map((f, i) => (            <div key={i} style={{ position: "relative", borderRadius: "8px", overflow: "hidden", aspectRatio: "1", backgroundColor: "#f0f4f0" }}>              {f.type === "image" ? <img src={f.preview} alt="" style={{ width: "100%", height: "100%", objectFit: "cover" }} /> : <video src={f.preview} style={{ width: "100%", height: "100%", objectFit: "cover" }} muted />}              <button onClick={() => setFiles(prev => prev.filter((_, j) => j !== i))} style={{ position: "absolute", top: "3px", right: "3px", backgroundColor: C.rouge, color: "#fff", border: "none", borderRadius: "50%", width: "18px", height: "18px", cursor: "pointer", fontSize: "11px", fontWeight: "900", lineHeight: "18px", padding: 0, textAlign: "center" }}>脳</button>              <div style={{ position: "absolute", bottom: "2px", left: "2px", backgroundColor: "rgba(0,0,0,0.55)", color: "#fff", borderRadius: "3px", padding: "1px 3px", fontSize: "8px" }}>{f.size}KB</div>            </div>          ))}        </div>      )}    </div>  );}// 鈹€鈹€鈹€ Navbar 鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€function Navbar({ setPage, user, setUser, panierCount, search, setSearch }) {  const [showSearch, setShowSearch] = useState(false);  return (    <nav style={S.nav}>      <div onClick={() => setPage("accueil")} style={{ cursor: "pointer", flexShrink: 0 }}>        <div style={S.logo}>HALIMAD</div>        <div style={S.navSub}>Madina 路 Conakry 路 Lab茅</div>      </div>      <div style={{ display: "flex", gap: "4px", alignItems: "center", flex: 1, justifyContent: "flex-end" }}>        {showSearch && (          <input            autoFocus            style={{ ...S.input, margin: 0, fontSize: "13px", padding: "6px 10px", maxWidth: "140px", borderColor: C.or }}            placeholder="Rechercher..."            value={search}            onChange={e => setSearch(e.target.value)}            onBlur={() => !search && setShowSearch(false)}          />        )}        <button style={S.navBtn(false)} onClick={() => { setShowSearch(s => !s); if (showSearch) setSearch(""); }}>馃攳</button>        {user ? (          <>            <button style={S.navBtn(false)} onClick={() => setPage("mes-commandes")}>              馃洅{panierCount > 0 && <span style={{ backgroundColor: C.or, color: C.vert, borderRadius: "8px", padding: "0 4px", fontSize: "9px", fontWeight: "900", marginLeft: "2px" }}>{panierCount}</span>}            </button>            <button style={S.navBtn(true)} onClick={() => setPage(user.role === "admin" ? "admin" : user.role === "vendeur" ? "vendeur" : "acheteur")}>              {user.role === "admin" ? "鈿欙笍" : user.role === "vendeur" ? "馃捈" : "馃懁"}            </button>            <button style={S.navBtn(false)} onClick={() => { setUser(null); setPage("accueil"); }}>鉁�</button>          </>        ) : (          <>            <button style={S.navBtn(false)} onClick={() => setPage("connexion")}>Connexion</button>            <button style={S.navBtn(true)} onClick={() => setPage("inscription")}>S'inscrire</button>          </>        )}      </div>    </nav>  );}// 鈹€鈹€鈹€ Bouton WhatsApp flottant 鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€function WAFloat() {  const [show, setShow] = useState(false);  return (    <div style={{ position: "fixed", bottom: "20px", right: "16px", zIndex: 9990, display: "flex", flexDirection: "column", alignItems: "flex-end", gap: "8px" }}>      {show && (        <div style={{ backgroundColor: C.blanc, borderRadius: "14px", padding: "14px", boxShadow: "0 4px 20px rgba(0,0,0,0.15)", width: "220px" }}>          <div style={{ fontWeight: "800", fontSize: "13px", color: C.vert, marginBottom: "8px" }}>馃挰 Contacter HALIMAD</div>          {[["馃洅 Passer une commande", "Bonjour HALIMAD ! Je veux passer une commande."], ["鉂� Poser une question", "Bonjour HALIMAD ! J'ai une question."], ["馃捈 Devenir vendeur", "Bonjour HALIMAD ! Je veux vendre sur votre plateforme."]].map(([label, msg]) => (            <a key={label} href={`https://wa.me/224624168584?text=${encodeURIComponent(msg)}`} target="_blank" rel="noreferrer" style={{ textDecoration: "none" }}>              <div style={{ padding: "8px 10px", borderRadius: "8px", backgroundColor: C.vertL, color: C.vert, fontSize: "12px", fontWeight: "600", marginBottom: "5px", cursor: "pointer" }}>{label}</div>            </a>          ))}        </div>      )}      <button onClick={() => setShow(s => !s)} style={{ backgroundColor: "#25D366", color: "#fff", border: "none", borderRadius: "50%", width: "52px", height: "52px", fontSize: "24px", cursor: "pointer", boxShadow: "0 4px 16px rgba(37,211,102,0.5)", display: "flex", alignItems: "center", justifyContent: "center" }}>        {show ? "鉁�" : "馃摫"}      </button>    </div>  );}// 鈹€鈹€鈹€ Carte produit 鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€function ProductCard({ p, onClick, onCommander, onShare }) {  const nouveau = isNew(p.created_at);  return (    <div style={S.card}      onMouseEnter={e => { e.currentTarget.style.transform = "translateY(-3px)"; e.currentTarget.style.boxShadow = "0 8px 20px rgba(0,0,0,0.12)"; }}      onMouseLeave={e => { e.currentTarget.style.transform = ""; e.currentTarget.style.boxShadow = "0 2px 8px rgba(0,0,0,0.07)"; }}    >      <div style={{ position: "relative", height: "160px", backgroundColor: "#f0f4f0", display: "flex", alignItems: "center", justifyContent: "center", overflow: "hidden" }} onClick={() => onClick(p)}>        {p.image_url ? <img src={p.image_url} alt={p.nom} style={{ width: "100%", height: "100%", objectFit: "cover" }} /> : <span style={{ fontSize: "50px" }}>{CAT_ICONS[p.categorie] || "馃摝"}</span>}        <div style={{ position: "absolute", top: "6px", right: "6px", backgroundColor: C.vert, color: "#fff", borderRadius: "10px", padding: "2px 6px", fontSize: "8px", fontWeight: "700" }}>鉁� V茅rifi茅</div>        {nouveau && <div style={{ position: "absolute", top: "6px", left: "6px", backgroundColor: "#E53935", color: "#fff", borderRadius: "5px", padding: "2px 6px", fontSize: "8px", fontWeight: "800" }}>馃啎 NOUVEAU</div>}        {!nouveau && <div style={{ position: "absolute", top: "6px", left: "6px", backgroundColor: C.or, color: C.vert, borderRadius: "5px", padding: "2px 6px", fontSize: "8px", fontWeight: "800" }}>{p.etat}</div>}        {/* Bouton partage */}        <button onClick={e => { e.stopPropagation(); onShare(p); }} style={{ position: "absolute", bottom: "6px", right: "6px", backgroundColor: "rgba(0,0,0,0.5)", color: "#fff", border: "none", borderRadius: "6px", padding: "3px 7px", fontSize: "11px", cursor: "pointer" }}>鈫� Partager</button>      </div>      <div style={{ padding: "10px" }} onClick={() => onClick(p)}>        <div style={{ fontWeight: "700", fontSize: "13px", marginBottom: "2px" }}>{p.nom}</div>        <div style={{ color: C.vert, fontWeight: "900", fontSize: "15px", marginBottom: "4px" }}>{fmt(p.prix)}</div>        <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: "4px" }}>          <Stars note={p.note_moy || 0} size={12} />          <span style={{ fontSize: "10px", color: "#aaa" }}>({p.nb_notes || 0})</span>        </div>        <div style={{ display: "flex", justifyContent: "space-between", fontSize: "10px", color: "#888" }}>          <span style={S.badge(C.vert)}>{p.categorie}</span>          <span>馃搷 {p.ville}</span>        </div>      </div>      <div style={{ padding: "0 10px 10px" }}>        <button style={S.waBtn} onClick={() => onCommander(p)}>馃洅 Commander</button>      </div>    </div>  );}// 鈹€鈹€鈹€ Page Accueil 鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€function PageAccueil({ setPage, setSelectedProduct, onCommander, onShare, search, setSearch }) {  const [produits, setProduits] = useState([]);  const [loading, setLoading] = useState(true);  const [ville, setVille] = useState("Toutes");  const [cat, setCat] = useState("Tout");  const [sort, setSort] = useState("recent");  const [dbOk, setDbOk] = useState(true);  useEffect(() => {    const fetch = async () => {      setLoading(true);      const { data, error } = await db.from("produits").select("*").eq("statut", "approuve").order("created_at", { ascending: false });      if (error || !data?.length) { setDbOk(false); setProduits(PRODUITS_DEMO); }      else { setDbOk(true); setProduits(data); }      setLoading(false);    };    fetch();  }, []);  // Compteur par cat茅gorie  const catCount = {};  produits.forEach(p => { catCount[p.categorie] = (catCount[p.categorie] || 0) + 1; });  let filtered = produits.filter(p =>    (ville === "Toutes" || p.ville === ville) &&    (cat === "Tout" || p.categorie === cat) &&    (p.nom.toLowerCase().includes(search.toLowerCase()) || p.categorie.toLowerCase().includes(search.toLowerCase()))  );  if (sort === "prix_asc") filtered = [...filtered].sort((a, b) => a.prix - b.prix);  if (sort === "prix_desc") filtered = [...filtered].sort((a, b) => b.prix - a.prix);  if (sort === "note") filtered = [...filtered].sort((a, b) => (b.note_moy || 0) - (a.note_moy || 0));  return (    <div>      <div style={S.hero}>        <div style={{ display: "inline-block", backgroundColor: C.or, color: C.vert, borderRadius: "20px", padding: "3px 12px", fontSize: "10px", fontWeight: "800", marginBottom: "8px" }}>馃洅 Marketplace V茅rifi茅e N掳1 de Guin茅e</div>        <h1 style={S.heroTitle}>HALIMAD</h1>        <p style={S.heroSub}>Achetez et vendez en toute confiance 路 Madina 路 Conakry 路 Lab茅</p>        <div style={{ maxWidth: "440px", margin: "0 auto 12px" }}>          <input style={{ width: "100%", padding: "10px 16px", borderRadius: "30px", border: `2px solid ${C.or}`, fontSize: "13px", outline: "none", fontFamily: "inherit", boxSizing: "border-box" }} placeholder="馃攳 Rechercher un produit..." value={search} onChange={e => setSearch(e.target.value)} />        </div>        <div style={{ display: "flex", justifyContent: "center", gap: "7px", flexWrap: "wrap" }}>          {["Toutes","Conakry","Lab茅"].map(v => (            <button key={v} style={{ backgroundColor: ville===v ? C.or : "rgba(255,255,255,0.15)", color: ville===v ? C.vert : "#fff", border: `2px solid ${ville===v ? C.or : "rgba(255,255,255,0.3)"}`, borderRadius: "30px", padding: "7px 16px", fontWeight: "700", cursor: "pointer", fontSize: "12px" }} onClick={() => setVille(v)}>馃搷 {v}</button>          ))}        </div>      </div>      <div style={S.wrap}>        {!dbOk && (          <div style={{ backgroundColor: C.orL, border: `1px solid ${C.or}`, borderRadius: "10px", padding: "10px 12px", marginBottom: "12px", fontSize: "11px", color: C.orange }}>            鈿狅笍 Mode d茅monstration 鈥� Connectez Supabase pour les vraies donn茅es.          </div>        )}        <div style={{ backgroundColor: C.orL, border: `1px solid ${C.or}`, borderRadius: "10px", padding: "10px 14px", marginBottom: "12px", display: "flex", justifyContent: "space-between", alignItems: "center", flexWrap: "wrap", gap: "7px" }}>          <div>            <div style={{ fontWeight: "800", color: C.vert, fontSize: "12px" }}>馃捈 Vendez sur HALIMAD</div>            <div style={{ fontSize: "10px", color: "#666" }}>Gratuit 路 V茅rification 24h 路 WhatsApp</div>          </div>          <button style={S.navBtn(true)} onClick={() => setPage("vendre")}>Vendre 鈫�</button>        </div>        {/* Cat茅gories avec compteurs */}        <div style={S.catBar}>          {CATS.map(c => (            <button key={c} style={S.catChip(cat===c)} onClick={() => setCat(c)}>              {CAT_ICONS[c]} {c}{c !== "Tout" && catCount[c] ? ` (${catCount[c]})` : ""}            </button>          ))}        </div>        <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center", marginBottom: "10px", flexWrap: "wrap", gap: "7px" }}>          <div style={{ fontWeight: "800", color: C.vert, fontSize: "14px" }}>馃泹锔� {loading ? "..." : `${filtered.length} produit${filtered.length !== 1 ? "s" : ""}`}</div>          <select style={{ padding: "5px 9px", borderRadius: "8px", border: "1.5px solid #E0E0E0", fontSize: "11px", fontFamily: "inherit", backgroundColor: C.blanc, cursor: "pointer" }} value={sort} onChange={e => setSort(e.target.value)}>            <option value="recent">Plus r茅cents</option>            <option value="prix_asc">Prix 鈫�</option>            <option value="prix_desc">Prix 鈫�</option>            <option value="note">Mieux not茅s</option>          </select>        </div>        <div style={S.grid}>          {loading ? Array(6).fill(0).map((_,i) => <Skeleton key={i} />) : filtered.map(p => (            <ProductCard key={p.id} p={p}              onClick={prod => { setSelectedProduct(prod); setPage("produit"); }}              onCommander={onCommander}              onShare={onShare}            />          ))}        </div>        {!loading && filtered.length === 0 && (          <div style={{ textAlign: "center", padding: "40px", color: "#bbb" }}>            <div style={{ fontSize: "38px" }}>馃攳</div>            <div style={{ marginTop: "8px" }}>Aucun produit trouv茅</div>            <button style={{ ...S.navBtn(true), marginTop: "10px" }} onClick={() => { setCat("Tout"); setSearch(""); }}>R茅initialiser</button>          </div>        )}      </div>    </div>  );}// 鈹€鈹€鈹€ Page Produit 鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€function PageProduit({ product, setPage, onCommander, onShare, onRate }) {  const [userNote, setUserNote] = useState(0);  const [noted, setNoted] = useState(false);  if (!product) return null;  const similaires = PRODUITS_DEMO.filter(p => p.categorie === product.categorie && p.id !== product.id).slice(0, 3);  const handleRate = (n) => {    setUserNote(n); setNoted(true);    onRate && onRate(product.id, n);  };  return (    <div style={{ maxWidth: "800px", margin: "0 auto", padding: "14px 10px" }}>      <button style={{ background: "none", border: "none", color: C.vert, fontWeight: "700", cursor: "pointer", fontSize: "13px", marginBottom: "12px", padding: 0 }} onClick={() => setPage("accueil")}>鈫� Retour</button>      <div style={{ backgroundColor: C.blanc, borderRadius: "14px", padding: "18px", boxShadow: "0 2px 10px rgba(0,0,0,0.07)", marginBottom: "12px" }}>        <div style={{ height: "240px", backgroundColor: "#f0f4f0", borderRadius: "10px", display: "flex", alignItems: "center", justifyContent: "center", overflow: "hidden", marginBottom: "10px" }}>          {product.image_url ? <img src={product.image_url} alt={product.nom} style={{ width: "100%", height: "100%", objectFit: "cover" }} /> : <span style={{ fontSize: "80px" }}>{CAT_ICONS[product.categorie] || "馃摝"}</span>}        </div>        <div style={{ display: "flex", justifyContent: "space-between", flexWrap: "wrap", gap: "5px", marginBottom: "6px" }}>          <h1 style={{ margin: 0, fontSize: "18px", fontWeight: "900" }}>{product.nom}</h1>          <span style={{ backgroundColor: C.vert, color: "#fff", borderRadius: "14px", padding: "2px 8px", fontSize: "9px", fontWeight: "700" }}>鉁� V茅rifi茅</span>        </div>        <div style={{ fontSize: "24px", fontWeight: "900", color: C.vert, marginBottom: "6px" }}>{fmt(product.prix)}</div>        {/* Notes */}        <div style={{ display: "flex", alignItems: "center", gap: "8px", marginBottom: "8px" }}>          <Stars note={product.note_moy || 0} size={16} />          <span style={{ fontSize: "12px", color: "#888" }}>{product.note_moy?.toFixed(1) || "0"} 路 {product.nb_notes || 0} avis</span>        </div>        <div style={{ display: "flex", gap: "5px", flexWrap: "wrap", marginBottom: "8px" }}>          {[product.categorie, product.ville, product.etat, product.code].map((t, i) => (            <span key={i} style={S.badge([C.vert, C.bleu, "#6A1B9A", C.or][i])}>{t}</span>          ))}          {isNew(product.created_at) && <span style={S.badge("#E53935")}>馃啎 Nouveau</span>}        </div>        <div style={{ fontSize: "12px", color: "#555", lineHeight: "1.6", padding: "9px", backgroundColor: C.gris, borderRadius: "8px", marginBottom: "10px" }}>{product.description}</div>        <div style={{ fontSize: "11px", color: "#888", marginBottom: "12px" }}>Vendeur : <strong>{product.vendeur_nom}</strong></div>        {/* Boutons action */}        <button style={{ ...S.waBtn, fontSize: "13px", padding: "11px", marginBottom: "8px" }} onClick={() => onCommander(product)}>馃洅 Commander maintenant</button>        {/* Partage WhatsApp */}        <button onClick={() => onShare(product)} style={{ width: "100%", padding: "9px", borderRadius: "8px", border: `1.5px solid ${C.vert}`, backgroundColor: C.blanc, color: C.vert, fontWeight: "700", fontSize: "12px", cursor: "pointer", display: "flex", alignItems: "center", justifyContent: "center", gap: "5px" }}>          鈫� Partager ce produit sur WhatsApp        </button>        {/* Garanties */}        <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: "6px", marginTop: "10px" }}>          {[["馃殮","Livraison Lab茅 48h"],["馃挼","Paiement r茅ception"],["馃攳","V茅rification autoris茅e"],["馃洝锔�","V茅rifi茅 脿 Madina"]].map(([ic,tx],i) => (            <div key={i} style={{ backgroundColor: C.vertL, borderRadius: "7px", padding: "7px 8px", display: "flex", alignItems: "center", gap: "4px", fontSize: "10px", fontWeight: "600", color: C.vert }}>              <span style={{ fontSize: "13px" }}>{ic}</span>{tx}            </div>          ))}        </div>        {/* Notation */}        <div style={{ marginTop: "16px", backgroundColor: C.vertL, borderRadius: "10px", padding: "12px" }}>          <div style={{ fontWeight: "800", fontSize: "12px", color: C.vert, marginBottom: "8px" }}>猸� Notez ce produit</div>          {noted ? (            <div style={{ color: C.vert, fontWeight: "700", fontSize: "13px" }}>鉁� Merci pour votre note : {userNote}/5 !</div>          ) : (            <>              <Stars note={userNote} size={24} interactive onRate={handleRate} />              <div style={{ fontSize: "10px", color: "#888", marginTop: "4px" }}>Appuyez sur une 茅toile pour noter</div>            </>          )}        </div>      </div>      {/* Produits similaires */}      {similaires.length > 0 && (        <div>          <div style={{ fontWeight: "800", fontSize: "14px", color: C.vert, marginBottom: "10px" }}>馃敆 Produits similaires</div>          <div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fill, minmax(160px, 1fr))", gap: "10px" }}>            {similaires.map(p => (              <div key={p.id} style={{ backgroundColor: C.blanc, borderRadius: "10px", overflow: "hidden", boxShadow: "0 1px 6px rgba(0,0,0,0.06)", cursor: "pointer" }} onClick={() => { setPage("produit"); }}>                <div style={{ height: "100px", backgroundColor: "#f0f4f0", display: "flex", alignItems: "center", justifyContent: "center", fontSize: "36px" }}>{CAT_ICONS[p.categorie]||"馃摝"}</div>                <div style={{ padding: "8px" }}>                  <div style={{ fontWeight: "700", fontSize: "11px", marginBottom: "2px" }}>{p.nom}</div>                  <div style={{ color: C.vert, fontWeight: "900", fontSize: "12px" }}>{fmt(p.prix)}</div>                  <Stars note={p.note_moy || 0} size={10} />                </div>              </div>            ))}          </div>        </div>      )}    </div>  );}// 鈹€鈹€鈹€ Page 脌 propos 鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€function PageAPropos({ setPage }) {  return (    <div style={{ maxWidth: "700px", margin: "0 auto", padding: "14px 10px" }}>      <button style={{ background: "none", border: "none", color: C.vert, fontWeight: "700", cursor: "pointer", fontSize: "13px", marginBottom: "12px", padding: 0 }} onClick={() => setPage("accueil")}>鈫� Retour</button>      <div style={{ backgroundColor: C.blanc, borderRadius: "14px", overflow: "hidden", boxShadow: "0 2px 10px rgba(0,0,0,0.07)", marginBottom: "12px" }}>        <div style={{ background: `linear-gradient(135deg, ${C.vert}, #2E7D32)`, padding: "32px 20px", textAlign: "center" }}>          <div style={{ color: C.or, fontSize: "36px", fontWeight: "900", letterSpacing: "3px", marginBottom: "4px" }}>HALIMAD</div>          <div style={{ color: "rgba(255,255,255,0.85)", fontSize: "13px" }}>La marketplace de confiance en Guin茅e</div>        </div>        <div style={{ padding: "20px" }}>          <div style={{ fontWeight: "800", fontSize: "16px", color: C.vert, marginBottom: "8px" }}>馃彧 Notre histoire</div>          <div style={{ fontSize: "13px", color: "#555", lineHeight: "1.7", marginBottom: "16px" }}>            HALIMAD est n茅e 脿 <strong>Madina, Conakry</strong> avec une mission simple : permettre 脿 chaque Guin茅en d'acheter et vendre en toute confiance, sans se faire arnaquer. Nous v茅rifions chaque produit et chaque vendeur avant publication.          </div>          <div style={{ fontWeight: "800", fontSize: "15px", color: C.vert, marginBottom: "10px" }}>馃搷 Nous trouver</div>          <div style={{ backgroundColor: C.vertL, borderRadius: "10px", padding: "14px", marginBottom: "16px" }}>            {[["馃搷", "Adresse", "March茅 de Madina, Conakry, Guin茅e"], ["馃摫", "WhatsApp", "+224 624 16 85 84"], ["馃晵", "Horaires", "Lundi鈥揝amedi, 8h鈥�20h"], ["馃實", "Zones", "Conakry 路 Lab茅 路 Guin茅e"]].map(([ic, k, v]) => (              <div key={k} style={{ display: "flex", gap: "10px", marginBottom: "8px", fontSize: "13px" }}>                <span style={{ fontSize: "16px" }}>{ic}</span>                <div><strong>{k} :</strong> {v}</div>              </div>            ))}          </div>          <div style={{ fontWeight: "800", fontSize: "15px", color: C.vert, marginBottom: "10px" }}>鉁� Nos engagements</div>          <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: "8px", marginBottom: "16px" }}>            {[["馃洝锔�", "Produits v茅rifi茅s"], ["馃挼", "Paiement 脿 r茅ception"], ["馃攳", "V茅rification physique autoris茅e"], ["馃殮", "Livraison Lab茅 48h"], ["馃摫", "Support WhatsApp"], ["馃啌", "Vente 100% gratuite"]].map(([ic, tx]) => (              <div key={tx} style={{ backgroundColor: C.vertL, borderRadius: "8px", padding: "9px 10px", display: "flex", alignItems: "center", gap: "6px", fontSize: "11px", fontWeight: "600", color: C.vert }}>                <span style={{ fontSize: "16px" }}>{ic}</span>{tx}              </div>            ))}          </div>          <div style={{ fontWeight: "800", fontSize: "15px", color: C.vert, marginBottom: "10px" }}>馃搳 HALIMAD en chiffres</div>          <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr 1fr", gap: "8px", marginBottom: "16px" }}>            {[["100+", "Produits v茅rifi茅s"], ["50+", "Vendeurs actifs"], ["2", "Villes couvertes"]].map(([n, l]) => (              <div key={l} style={{ backgroundColor: C.gris, borderRadius: "10px", padding: "12px", textAlign: "center" }}>                <div style={{ fontSize: "20px", fontWeight: "900", color: C.vert }}>{n}</div>                <div style={{ fontSize: "10px", color: "#888", marginTop: "2px" }}>{l}</div>              </div>            ))}          </div>          <a href="https://wa.me/224624168584?text=Bonjour HALIMAD ! Je veux en savoir plus." target="_blank" rel="noreferrer" style={{ textDecoration: "none" }}>            <button style={{ ...S.waBtn, fontSize: "14px", padding: "12px" }}>馃摫 Nous contacter sur WhatsApp</button>          </a>        </div>      </div>    </div>  );}// 鈹€鈹€鈹€ Modal Commande 鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€function ModalCommande({ produit, user, onClose, onSuccess, addToast }) {  const [form, setForm] = useState({ nom: user?.nom||"", tel: user?.telephone||"", ville: user?.ville||"Conakry", adresse: "", quantite: 1 });  const [step, setStep] = useState(1);  const [saving, setSaving] = useState(false);  const [cmdCode] = useState(genCode());  const total = produit.prix * form.quantite;  const confirmer = async () => {    setSaving(true); setStep(3);    await db.from("commandes").insert({ produit_nom: produit.nom, produit_code: produit.code || produit.id, prix: total, acheteur_nom: form.nom, acheteur_tel: form.tel, ville: form.ville, adresse: form.adresse, statut: "en_attente", code: cmdCode });    const msg = encodeURIComponent(`馃洅 COMMANDE HALIMAD\n\n馃彿锔� ${produit.nom}\n馃挵 ${fmt(total)} x${form.quantite}\n馃懁 ${form.nom}\n馃摓 +224${form.tel}\n馃搷 ${form.ville} 鈥� ${form.adresse}\n馃啍 ${cmdCode}`);    window.open(`https://wa.me/224624168584?text=${msg}`, "_blank");    onSuccess({ id: cmdCode, produit_nom: produit.nom, prix: total, acheteur_nom: form.nom, acheteur_tel: form.tel, ville: form.ville, adresse: form.adresse, statut: "en_attente", code: cmdCode });    addToast("Commande envoy茅e ! On vous contacte sous 1h", "success", "馃帀");    setSaving(false);  };  return (    <div style={S.modal} onClick={onClose}>      <div style={S.modalBox} onClick={e => e.stopPropagation()}>        {step === 1 && <>          <div style={{ display: "flex", justifyContent: "space-between", marginBottom: "12px" }}>            <div style={{ fontSize: "15px", fontWeight: "900", color: C.vert }}>馃洅 Commander</div>            <button onClick={onClose} style={{ background: "none", border: "none", fontSize: "20px", cursor: "pointer", color: "#aaa" }}>脳</button>          </div>          <div style={{ backgroundColor: C.vertL, borderRadius: "9px", padding: "9px 11px", marginBottom: "11px", display: "flex", gap: "9px", alignItems: "center" }}>            <span style={{ fontSize: "24px" }}>{CAT_ICONS[produit.categorie]||"馃摝"}</span>            <div><div style={{ fontWeight: "700", fontSize: "12px" }}>{produit.nom}</div><div style={{ color: C.vert, fontWeight: "900", fontSize: "13px" }}>{fmt(produit.prix)}</div></div>          </div>          {[["Nom complet *", "Mamadou Diallo", "nom", "text"], ["T茅l茅phone WhatsApp *", "622 00 11 22", "tel", "tel"], ["Adresse de livraison *", "Quartier, rue, rep猫re...", "adresse", "text"]].map(([lbl,ph,key,type]) => (            <div key={key}><label style={S.label}>{lbl}</label><input style={S.input} type={type} placeholder={ph} value={form[key]} onChange={e => setForm({...form, [key]: e.target.value})} /></div>          ))}          <label style={S.label}>Ville</label>          <select style={S.sel} value={form.ville} onChange={e => setForm({...form, ville: e.target.value})}><option>Conakry</option><option>Lab茅</option></select>          <div style={{ display: "flex", alignItems: "center", gap: "10px", marginBottom: "10px" }}>            <label style={{ ...S.label, margin: 0 }}>Qt茅 :</label>            {["-","+"].map((op,i) => <button key={op} onClick={() => setForm({...form, quantite: Math.max(1, form.quantite+(i===0?-1:1))})} style={{ width: "28px", height: "28px", borderRadius: "50%", border: `2px solid ${C.vert}`, background: "none", fontSize: "15px", cursor: "pointer", color: C.vert, fontWeight: "900" }}>{op}</button>)}            <span style={{ fontSize: "15px", fontWeight: "800" }}>{form.quantite}</span>            <span style={{ fontSize: "12px", color: C.vert, fontWeight: "900" }}>{fmt(total)}</span>          </div>          <div style={{ backgroundColor: C.orL, borderRadius: "7px", padding: "8px 10px", marginBottom: "10px", fontSize: "11px", color: C.orange }}>馃挼 Paiement 脿 la r茅ception uniquement</div>          <button style={S.btn} onClick={() => { if (!form.nom || !form.tel || !form.adresse) return; setStep(2); }}>Voir le r茅capitulatif 鈫�</button>        </>}        {step === 2 && <>          <div style={{ display: "flex", justifyContent: "space-between", marginBottom: "12px" }}>            <div style={{ fontSize: "15px", fontWeight: "900", color: C.vert }}>馃搵 R茅capitulatif</div>            <button onClick={() => setStep(1)} style={{ background: "none", border: "none", color: C.vert, fontWeight: "700", cursor: "pointer", fontSize: "11px" }}>鈫� Modifier</button>          </div>          {[["Produit",produit.nom],["Total",fmt(total)],["Acheteur",form.nom],["Tel","+224"+form.tel],["Livraison",`${form.ville} 鈥� ${form.adresse}`],["R茅f.",cmdCode]].map(([k,v]) => (            <div key={k} style={{ display: "flex", justifyContent: "space-between", padding: "7px 0", borderBottom: "1px solid #F0F0F0", fontSize: "12px" }}>              <span style={{ color: "#888" }}>{k}</span><span style={{ fontWeight: "700" }}>{v}</span>            </div>          ))}          <div style={{ backgroundColor: C.vertL, borderRadius: "8px", padding: "9px", margin: "10px 0", fontSize: "11px", color: C.vert }}>馃摫 Message WhatsApp envoy茅 脿 HALIMAD.</div>          <button style={{ ...S.btn, opacity: saving ? 0.7 : 1 }} onClick={confirmer} disabled={saving}>{saving ? "Envoi..." : "鉁� Confirmer & envoyer"}</button>        </>}        {step === 3 && (          <div style={{ textAlign: "center", padding: "20px 0" }}>            <div style={{ fontSize: "44px", marginBottom: "9px" }}>馃帀</div>            <div style={{ fontSize: "17px", fontWeight: "900", color: C.vert, marginBottom: "5px" }}>Commande envoy茅e !</div>            <div style={{ color: "#666", fontSize: "12px", marginBottom: "10px" }}>Nous vous contactons sous 1h sur WhatsApp.</div>            <div style={{ backgroundColor: C.orL, borderRadius: "8px", padding: "9px", marginBottom: "12px" }}>              <div style={{ fontSize: "9px", color: "#888" }}>R茅f茅rence</div>              <div style={{ fontSize: "18px", fontWeight: "900", color: C.vert }}>{cmdCode}</div>            </div>            <button style={S.btn} onClick={onClose}>Fermer</button>          </div>        )}      </div>    </div>  );}// 鈹€鈹€鈹€ Page Vendre 鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€function PageVendre({ setPage, user, addToast }) {  const [form, setForm] = useState({ nom: "", prix: "", ville: "Conakry", categorie: "T茅l茅phones", etat: "Bon 茅tat", description: "", tel: user?.telephone || "" });  const [photos, setPhotos] = useState([]);  const [videos, setVideos] = useState([]);  const [error, setError] = useState("");  const [saving, setSaving] = useState(false);  const [submitted, setSubmitted] = useState(false);  const [code] = useState(genCode());  const submit = async () => {    if (!form.nom.trim()) { setError("鈿狅笍 Nom du produit obligatoire."); return; }    if (!form.prix || isNaN(form.prix) || Number(form.prix) <= 0) { setError("鈿狅笍 Prix invalide."); return; }    if (!form.tel.trim()) { setError("鈿狅笍 T茅l茅phone WhatsApp obligatoire."); return; }    setError(""); setSaving(true);    await db.from("produits").insert({ nom: form.nom, prix: Number(form.prix), ville: form.ville, categorie: form.categorie, etat: form.etat, description: form.description, vendeur_nom: user?.nom || "Vendeur", vendeur_tel: form.tel, code, statut: "en_attente", image_url: null, note_moy: 0, nb_notes: 0 });    setSaving(false); setSubmitted(true);    addToast("Produit soumis ! V茅rification sous 24h", "success", "馃摝");  };  if (submitted) {    const msg = encodeURIComponent(`馃摝 NOUVEAU PRODUIT HALIMAD\n馃泹锔� ${form.nom}\n馃挵 ${fmt(Number(form.prix))}\n馃搷 ${form.ville} 路 ${form.categorie}\n馃彿锔� ${code}\n馃摲 ${photos.length} photo(s)\n馃摓 +224${form.tel}`);    return (      <div style={S.wrap}>        <div style={{ backgroundColor: C.blanc, borderRadius: "14px", padding: "26px", textAlign: "center", maxWidth: "400px", margin: "0 auto", boxShadow: "0 4px 16px rgba(0,0,0,0.07)" }}>          <div style={{ fontSize: "44px", marginBottom: "9px" }}>馃帀</div>          <div style={{ fontSize: "17px", fontWeight: "900", color: C.vert, marginBottom: "6px" }}>Produit soumis !</div>          <div style={{ color: "#666", marginBottom: "10px", fontSize: "12px" }}>Notre 茅quipe v茅rifie sous 24h et vous contacte.</div>          <div style={{ backgroundColor: C.orL, borderRadius: "8px", padding: "9px", marginBottom: "14px" }}>            <div style={{ fontSize: "9px", color: "#888" }}>Code de suivi</div>            <div style={{ fontSize: "20px", fontWeight: "900", color: C.vert }}>{code}</div>          </div>          <a href={`https://wa.me/224624168584?text=${msg}`} target="_blank" rel="noreferrer" style={{ textDecoration: "none" }}>            <button style={{ ...S.waBtn, marginBottom: "8px", padding: "10px" }}>馃摫 Notifier HALIMAD</button>          </a>          <button style={S.btn} onClick={() => setPage("accueil")}>Retour 脿 l'accueil</button>        </div>      </div>    );  }  return (    <div style={S.wrap}>      <div style={{ fontSize: "18px", fontWeight: "900", color: C.vert, marginBottom: "3px" }}>馃捈 Vendre sur HALIMAD</div>      <div style={{ color: "#888", marginBottom: "16px", fontSize: "11px" }}>Gratuit 路 V茅rification 24h 路 Paiement 脿 la r茅ception</div>      <div style={{ backgroundColor: C.blanc, borderRadius: "14px", padding: "18px", boxShadow: "0 2px 8px rgba(0,0,0,0.06)", maxWidth: "540px" }}>        {error && <div style={{ backgroundColor: "#FFEBEE", color: C.rouge, borderRadius: "7px", padding: "9px 11px", fontSize: "12px", marginBottom: "10px", fontWeight: "600" }}>{error}</div>}        <label style={S.label}>Nom du produit *</label>        <input style={S.input} placeholder="ex: iPhone 14 Pro Max 256Go" value={form.nom} onChange={e => setForm({...form, nom: e.target.value})} />        <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: "8px" }}>          <div>            <label style={S.label}>Prix en GNF *</label>            <input style={S.input} type="number" placeholder="8500000" value={form.prix} onChange={e => setForm({...form, prix: e.target.value})} />            {form.prix && !isNaN(form.prix) && Number(form.prix) > 0 && <div style={{ marginTop: "-6px", marginBottom: "10px", fontSize: "11px", color: C.vert, fontWeight: "700" }}>= {fmt(Number(form.prix))}</div>}          </div>          <div>            <label style={S.label}>脡tat</label>            <select style={S.sel} value={form.etat} onChange={e => setForm({...form, etat: e.target.value})}>              {["Neuf","Tr猫s bon 茅tat","Bon 茅tat","脡tat correct"].map(e => <option key={e}>{e}</option>)}            </select>          </div>        </div>        <div style={{ display: "grid", gridTemplateColumns: "1fr 1fr", gap: "8px" }}>          <div>            <label style={S.label}>Cat茅gorie</label>            <select style={S.sel} value={form.categorie} onChange={e => setForm({...form, categorie: e.target.value})}>              {["T茅l茅phones","Mode","脡lectronique","V茅hicules","Mobilier","Alimentation","Autre"].map(c => <option key={c}>{c}</option>)}            </select>          </div>          <div>            <label style={S.label}>Ville</label>            <select style={S.sel} value={form.ville} onChange={e => setForm({...form, ville: e.target.value})}><option>Conakry</option><option>Lab茅</option></select>          </div>        </div>        <label style={S.label}>Description</label>        <textarea style={{ ...S.input, minHeight: "65px", resize: "vertical" }} placeholder="D茅crivez votre produit..." value={form.description} onChange={e => setForm({...form, description: e.target.value})} />        <label style={S.label}>T茅l茅phone WhatsApp *</label>        <input style={S.input} placeholder="622 00 11 22" value={form.tel} onChange={e => setForm({...form, tel: e.target.value})} />        <div style={{ borderTop: "1px solid #F0F0F0", paddingTop: "14px" }}>          <UploadZone label="馃摲 Photos du produit (max 5)" files={photos} setFiles={setPhotos} maxFiles={5} type="photo" />          <UploadZone label="馃帴 Vid茅o d茅monstration (optionnel)" files={videos} setFiles={setVideos} maxFiles={1} type="video" />        </div>        <div style={{ backgroundColor: C.vertL, borderRadius: "8px", padding: "9px 11px", marginBottom: "10px", fontSize: "11px", color: C.vert }}>          馃洝锔� Notre 茅quipe vous contacte sur WhatsApp sous 24h.        </div>        <button style={{ ...S.btn, opacity: saving ? 0.7 : 1 }} onClick={submit} disabled={saving}>          {saving ? "鈴� Envoi..." : `鉁� Soumettre${photos.length > 0 ? ` (${photos.length} photo${photos.length > 1 ? "s" : ""})` : ""} 鈫抈}        </button>      </div>    </div>  );}// 鈹€鈹€鈹€ Auth 鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€function PageConnexion({ setPage, setUser, addToast }) {  const [form, setForm] = useState({ email: "", password: "" });  const [error, setError] = useState("");  const login = () => {    const u = DEMO_USERS.find(u => u.email === form.email && u.password === form.password);    if (!u) { setError("Email ou mot de passe incorrect."); return; }    setUser(u); addToast(`Bienvenue ${u.nom} !`, "success", "馃憢");    setPage(u.role === "admin" ? "admin" : u.role === "vendeur" ? "vendeur" : "acheteur");  };  return (    <div style={S.wrap}>      <div style={S.authCard}>        <div style={{ fontSize: "17px", fontWeight: "900", color: C.vert, textAlign: "center", marginBottom: "3px" }}>Connexion</div>        <div style={{ color: "#888", fontSize: "11px", textAlign: "center", marginBottom: "16px" }}>Bienvenue sur HALIMAD</div>        {error && <div style={{ backgroundColor: "#FFEBEE", color: C.rouge, borderRadius: "7px", padding: "7px", fontSize: "11px", marginBottom: "9px", textAlign: "center" }}>{error}</div>}        <div style={{ backgroundColor: C.vertL, borderRadius: "7px", padding: "8px", marginBottom: "10px", fontSize: "10px", color: C.vert }}>          Admin : admin@halimad.com / admin123<br />Vendeur : vendeur@halimad.com / vendeur123<br />Acheteur : acheteur@halimad.com / acheteur123        </div>        <label style={S.label}>Email</label>        <input style={S.input} type="email" value={form.email} onChange={e => setForm({...form, email: e.target.value})} placeholder="votre@email.com" />        <label style={S.label}>Mot de passe</label>        <input style={S.input} type="password" value={form.password} onChange={e => setForm({...form, password: e.target.value})} placeholder="鈥⑩€⑩€⑩€⑩€⑩€⑩€⑩€�" onKeyDown={e => e.key === "Enter" && login()} />        <button style={S.btn} onClick={login}>Se connecter</button>        <div style={{ textAlign: "center", marginTop: "10px", fontSize: "11px", color: "#666" }}>Pas de compte ? <span style={{ color: C.vert, fontWeight: "700", cursor: "pointer" }} onClick={() => setPage("inscription")}>S'inscrire</span></div>      </div>    </div>  );}function PageInscription({ setPage, setUser, addToast }) {  const [form, setForm] = useState({ nom: "", email: "", telephone: "", ville: "Conakry", role: "acheteur", password: "", confirm: "" });  const [error, setError] = useState("");  const submit = () => {    if (!form.nom || !form.email || !form.telephone || !form.password) { setError("Remplissez tous les champs."); return; }    if (form.password.length < 6) { setError("Mot de passe trop court."); return; }    if (form.password !== form.confirm) { setError("Mots de passe diff茅rents."); return; }    setUser({ nom: form.nom, email: form.email, telephone: form.telephone, ville: form.ville, role: form.role });    addToast("Compte cr茅茅 !", "success", "馃帀");    setPage(form.role === "vendeur" ? "vendeur" : "acheteur");  };  return (    <div style={S.wrap}>      <div style={S.authCard}>        <div style={{ fontSize: "17px", fontWeight: "900", color: C.vert, textAlign: "center", marginBottom: "3px" }}>Cr茅er un compte</div>        <div style={{ color: "#888", fontSize: "11px", textAlign: "center", marginBottom: "16px" }}>Rejoignez HALIMAD gratuitement</div>        {error && <div style={{ backgroundColor: "#FFEBEE", color: C.rouge, borderRadius: "7px", padding: "7px", fontSize: "11px", marginBottom: "9px", textAlign: "center" }}>{error}</div>}        {[{r:"acheteur",icon:"馃泹锔�",desc:"Acheter"},{r:"vendeur",icon:"馃捈",desc:"Vendre"}].map(({r,icon,desc}) => (          <div key={r} style={{ border: `2px solid ${form.role===r ? C.vert : "#E0E0E0"}`, borderRadius: "9px", padding: "10px", cursor: "pointer", backgroundColor: form.role===r ? C.vertL : C.blanc, marginBottom: "7px" }} onClick={() => setForm({...form, role: r})}>            <div style={{ fontWeight: "700", fontSize: "12px" }}>{icon} {r.charAt(0).toUpperCase()+r.slice(1)}</div>            <div style={{ fontSize: "10px", color: "#888" }}>{desc} des produits</div>          </div>        ))}        {[["Nom *","Mamadou Diallo","nom","text"],["Email *","ex@email.com","email","email"],["T茅l茅phone *","622 00 11 22","telephone","tel"]].map(([lbl,ph,key,type]) => (          <div key={key}><label style={S.label}>{lbl}</label><input style={S.input} type={type} placeholder={ph} value={form[key]} onChange={e => setForm({...form, [key]: e.target.value})} /></div>        ))}        <label style={S.label}>Ville</label>        <select style={S.sel} value={form.ville} onChange={e => setForm({...form, ville: e.target.value})}><option>Conakry</option><option>Lab茅</option></select>        <label style={S.label}>Mot de passe *</label>        <input style={S.input} type="password" placeholder="Min. 6 caract猫res" value={form.password} onChange={e => setForm({...form, password: e.target.value})} />        <label style={S.label}>Confirmer *</label>        <input style={S.input} type="password" placeholder="R茅p茅tez" value={form.confirm} onChange={e => setForm({...form, confirm: e.target.value})} />        <button style={S.btn} onClick={submit}>Cr茅er mon compte</button>        <div style={{ textAlign: "center", marginTop: "9px", fontSize: "11px", color: "#666" }}>D茅j脿 un compte ? <span style={{ color: C.vert, fontWeight: "700", cursor: "pointer" }} onClick={() => setPage("connexion")}>Se connecter</span></div>      </div>    </div>  );}// 鈹€鈹€鈹€ Dashboard Admin 鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€function DashboardAdmin({ addToast }) {  const [tab, setTab] = useState("commandes");  const [commandes, setCommandes] = useState([]);  const [produits, setProduits] = useState([]);  const [vendeurs, setVendeurs] = useState([]);  const [loading, setLoading] = useState(true);  useEffect(() => {    const fetchAll = async () => {      setLoading(true);      const [{ data: cmds }, { data: prods }, { data: vends }] = await Promise.all([        db.from("commandes").select("*").order ? db.from("commandes").select("*").order("created_at", { ascending: false }) : Promise.resolve({ data: [] }),        db.from("produits").select("*").order ? db.from("produits").select("*").order("created_at", { ascending: false }) : Promise.resolve({ data: [] }),        db.from("vendeurs").select("*").order ? db.from("vendeurs").select("*").order("created_at", { ascending: false }) : Promise.resolve({ data: [] }),      ]);      setCommandes(cmds || []); setProduits(prods || []); setVendeurs(vends || []);      setLoading(false);    };    fetchAll();  }, []);  const pending = produits.filter(p => p.statut === "en_attente");  const approuves = produits.filter(p => p.statut === "approuve");  const totalCA = commandes.filter(c => c.statut === "livree").reduce((s, c) => s + (c.prix || 0), 0);  const approuver = async (p) => {    await db.from("produits").update({ statut: "approuve" }).eq("id", p.id);    setProduits(prev => prev.map(x => x.id === p.id ? {...x, statut: "approuve"} : x));    window.open(`https://wa.me/224${p.vendeur_tel}?text=${encodeURIComponent(`鉁� Bonjour ${p.vendeur_nom} ! Votre produit "${p.nom}" (${p.code}) est EN LIGNE sur HALIMAD ! 馃帀`)}`, "_blank");    addToast(`${p.nom} approuv茅 !`, "success", "鉁�");  };  const rejeter = async (p) => {    await db.from("produits").update({ statut: "rejete" }).eq("id", p.id);    setProduits(prev => prev.map(x => x.id === p.id ? {...x, statut: "rejete"} : x));    addToast(`${p.nom} rejet茅`, "error", "鉂�");  };  const supprimer = async (p) => {    await db.from("produits").delete().eq("id", p.id);    setProduits(prev => prev.filter(x => x.id !== p.id));    addToast("Produit supprim茅", "error", "馃棏锔�");  };  const updateCmd = async (id, newStatut) => {    await db.from("commandes").update({ statut: newStatut }).eq("id", id);    const cmd = commandes.find(c => c.id === id);    const s = STATUTS.find(x => x.key === newStatut);    setCommandes(prev => prev.map(c => c.id === id ? {...c, statut: newStatut} : c));    if (cmd) window.open(`https://wa.me/224${cmd.acheteur_tel}?text=${encodeURIComponent(`馃摝 HALIMAD 鈥� Bonjour ${cmd.acheteur_nom} ! Commande ${cmd.code} : ${s.icon} ${s.label}`)}`, "_blank");    addToast(`鈫� ${s.label}`, "success", s.icon);  };  const toggleVendeur = async (v) => {    const ns = v.statut === "actif" ? "suspendu" : "actif";    await db.from("vendeurs").update({ statut: ns }).eq("id", v.id);    setVendeurs(prev => prev.map(x => x.id === v.id ? {...x, statut: ns} : x));    addToast(`${v.nom} ${ns}`, ns === "actif" ? "success" : "error", ns === "actif" ? "鉁�" : "鈿狅笍");  };  if (loading) return <div style={{ textAlign: "center", padding: "50px", color: C.vert, fontWeight: "700" }}>鈴� Chargement Supabase...</div>;  return (    <div>      <div style={S.dashHead}>        <div style={{ fontSize: "17px", fontWeight: "900" }}>鈿欙笍 Dashboard Admin</div>        <div style={{ fontSize: "10px", opacity: 0.8, marginTop: "3px" }}>HALIMAD 路 Supabase 路 +224 624 16 85 84</div>      </div>      <div style={S.wrap}>        <div style={S.statGrid}>          {[{ n: approuves.length, l: "En ligne" }, { n: pending.length, l: "脌 valider", a: pending.length > 0 }, { n: commandes.length, l: "Commandes" }, { n: vendeurs.filter(v=>v.statut==="actif").length, l: "Vendeurs" }, { n: fmt(totalCA).replace(" GNF",""), l: "CA (GNF)" }].map((s, i) => (            <div key={i} style={{ ...S.statCard, border: s.a ? `2px solid ${C.or}` : "none" }}>              <div style={{ fontSize: "18px", fontWeight: "900", color: s.a ? C.or : C.vert }}>{s.n}</div>              <div style={{ fontSize: "10px", color: "#888", marginTop: "2px" }}>{s.l}</div>            </div>          ))}        </div>        <div style={{ display: "flex", gap: "5px", marginBottom: "14px", flexWrap: "wrap" }}>          {[["commandes",`馃摝 (${commandes.length})`],["pending",`鈴� (${pending.length})`],["enligne",`鉁� (${approuves.length})`],["vendeurs",`馃懁 (${vendeurs.length})`]].map(([k,l]) => (            <button key={k} style={S.tab(tab===k)} onClick={() => setTab(k)}>{l}</button>          ))}        </div>        {tab === "commandes" && (commandes.length === 0          ? <div style={{ textAlign: "center", padding: "32px", color: "#aaa" }}>Aucune commande</div>          : commandes.map(c => {            const s = STATUTS.find(x=>x.key===c.statut)||STATUTS[0];            return (              <div key={c.id} style={S.row}>                <div style={{ display: "flex", justifyContent: "space-between", flexWrap: "wrap", gap: "7px" }}>                  <div>                    <div style={{ fontWeight: "700", fontSize: "12px" }}>{c.produit_nom}</div>                    <div style={{ fontSize: "10px", color: "#888" }}>{c.code} 路 {c.acheteur_nom}</div>                    <div style={{ fontSize: "13px", fontWeight: "900", color: C.vert }}>{fmt(c.prix)}</div>                    <div style={{ fontSize: "10px", color: "#aaa" }}>馃搷 {c.ville} 鈥� {c.adresse}</div>                  </div>                  <div style={{ display: "flex", flexDirection: "column", gap: "4px" }}>                    <select style={{ padding: "4px 7px", borderRadius: "6px", border: `1.5px solid ${C.vert}`, fontSize: "10px", fontFamily: "inherit", cursor: "pointer", color: C.vert, fontWeight: "700" }} value={c.statut} onChange={e => updateCmd(c.id, e.target.value)}>                      {STATUTS.map(s => <option key={s.key} value={s.key}>{s.icon} {s.label}</option>)}                    </select>                    <a href={`https://wa.me/224${c.acheteur_tel}`} target="_blank" rel="noreferrer" style={{ textDecoration: "none" }}>                      <button style={{ backgroundColor: "#25D366", color: "#fff", border: "none", borderRadius: "5px", padding: "4px 8px", fontWeight: "700", cursor: "pointer", fontSize: "10px", width: "100%" }}>馃摫 WA</button>                    </a>                  </div>                </div>              </div>            );          })        )}        {tab === "pending" && (pending.length === 0          ? <div style={{ textAlign: "center", padding: "32px", color: "#aaa" }}>鉁� Aucun produit en attente</div>          : pending.map(p => (            <div key={p.id} style={S.row}>              <div style={{ display: "flex", justifyContent: "space-between", flexWrap: "wrap", gap: "7px" }}>                <div>                  <div style={{ fontWeight: "700", fontSize: "12px" }}>{CAT_ICONS[p.categorie]||"馃摝"} {p.nom}</div>                  <div style={{ fontSize: "10px", color: "#888" }}>{p.vendeur_nom} 路 {p.ville} 路 {p.code}</div>                  <div style={{ fontSize: "13px", fontWeight: "900", color: C.vert }}>{fmt(p.prix)}</div>                </div>                <div style={{ display: "flex", gap: "4px", flexWrap: "wrap" }}>                  <button style={{ backgroundColor: C.vert, color: "#fff", border: "none", borderRadius: "5px", padding: "5px 9px", fontWeight: "700", cursor: "pointer", fontSize: "10px" }} onClick={() => approuver(p)}>鉁� Approuver</button>                  <button style={{ backgroundColor: C.rouge, color: "#fff", border: "none", borderRadius: "5px", padding: "5px 9px", fontWeight: "700", cursor: "pointer", fontSize: "10px" }} onClick={() => rejeter(p)}>鉁� Rejeter</button>                </div>              </div>            </div>          ))        )}        {tab === "enligne" && (approuves.length === 0          ? <div style={{ textAlign: "center", padding: "32px", color: "#aaa" }}>Aucun produit en ligne</div>          : approuves.map(p => (            <div key={p.id} style={S.row}>              <div style={{ display: "flex", justifyContent: "space-between", flexWrap: "wrap", gap: "7px" }}>                <div>                  <div style={{ fontWeight: "700", fontSize: "12px" }}>{CAT_ICONS[p.categorie]||"馃摝"} {p.nom}</div>                  <div style={{ fontSize: "10px", color: "#888" }}>{p.ville} 路 {p.code}</div>                  <div style={{ fontSize: "13px", fontWeight: "900", color: C.vert }}>{fmt(p.prix)}</div>                  <Stars note={p.note_moy||0} size={11} />                </div>                <div style={{ display: "flex", gap: "4px", flexWrap: "wrap", alignItems: "center" }}>                  <span style={S.badge(C.vert, C.vertL)}>鉁� En ligne</span>                  <button style={{ backgroundColor: C.rouge, color: "#fff", border: "none", borderRadius: "5px", padding: "5px 9px", fontWeight: "700", cursor: "pointer", fontSize: "10px" }} onClick={() => supprimer(p)}>馃棏锔�</button>                </div>              </div>            </div>          ))        )}        {tab === "vendeurs" && (vendeurs.length === 0          ? <div style={{ textAlign: "center", padding: "32px", color: "#aaa" }}>Aucun vendeur</div>          : vendeurs.map(v => (            <div key={v.id} style={S.row}>              <div style={{ display: "flex", justifyContent: "space-between", flexWrap: "wrap", gap: "7px" }}>                <div>                  <div style={{ fontWeight: "700", fontSize: "12px" }}>馃懁 {v.nom}</div>                  <div style={{ fontSize: "10px", color: "#888" }}>馃搷 {v.ville} 路 馃摫 +224{v.tel}</div>                  <span style={{ backgroundColor: v.statut==="actif" ? C.vertL : "#FFEBEE", color: v.statut==="actif" ? C.vert : C.rouge, borderRadius: "5px", padding: "2px 6px", fontSize: "9px", fontWeight: "700", marginTop: "3px", display: "inline-block" }}>                    {v.statut === "actif" ? "鉁� Actif" : "鈿狅笍 Suspendu"}                  </span>                </div>                <div style={{ display: "flex", flexDirection: "column", gap: "4px" }}>                  <a href={`https://wa.me/224${v.tel}`} target="_blank" rel="noreferrer" style={{ textDecoration: "none" }}>                    <button style={{ backgroundColor: "#25D366", color: "#fff", border: "none", borderRadius: "5px", padding: "4px 8px", fontWeight: "700", cursor: "pointer", fontSize: "10px", width: "100%" }}>馃摫 WA</button>                  </a>                  <button style={{ backgroundColor: v.statut==="actif" ? C.orange : C.vert, color: "#fff", border: "none", borderRadius: "5px", padding: "4px 8px", fontWeight: "700", cursor: "pointer", fontSize: "10px" }} onClick={() => toggleVendeur(v)}>                    {v.statut === "actif" ? "鈿狅笍 Suspendre" : "鉁� R茅activer"}                  </button>                  <button style={{ backgroundColor: C.rouge, color: "#fff", border: "none", borderRadius: "5px", padding: "4px 8px", fontWeight: "700", cursor: "pointer", fontSize: "10px" }} onClick={async () => { await db.from("vendeurs").delete().eq("id", v.id); setVendeurs(prev => prev.filter(x => x.id !== v.id)); }}>馃棏锔� Suppr.</button>                </div>              </div>            </div>          ))        )}      </div>    </div>  );}// 鈹€鈹€鈹€ Dashboards Vendeur & Acheteur 鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€function DashboardVendeur({ user, setPage }) {  const [produits, setProduits] = useState([]);  useEffect(() => { db.from("produits").select("*").eq("vendeur_tel", user?.telephone||"").then(({ data }) => setProduits(data||[])); }, []);  return (    <div>      <div style={S.dashHead}><div style={{ fontSize: "17px", fontWeight: "900" }}>馃捈 Espace Vendeur</div><div style={{ fontSize: "10px", opacity: 0.8 }}>{user?.nom}</div></div>      <div style={S.wrap}>        <div style={S.statGrid}>          {[{ n: produits.length, l: "Produits" }, { n: produits.filter(p=>p.statut==="approuve").length, l: "En ligne" }, { n: produits.filter(p=>p.statut==="en_attente").length, l: "En attente" }].map((s,i) => (            <div key={i} style={S.statCard}><div style={{ fontSize: "18px", fontWeight: "900", color: C.vert }}>{s.n}</div><div style={{ fontSize: "10px", color: "#888" }}>{s.l}</div></div>          ))}        </div>        <button style={{ ...S.btn, maxWidth: "180px", marginBottom: "14px", fontSize: "12px" }} onClick={() => setPage("vendre")}>+ Ajouter produit</button>        {produits.map(p => (          <div key={p.id} style={S.row}>            <div style={{ display: "flex", justifyContent: "space-between", flexWrap: "wrap", gap: "6px" }}>              <div><div style={{ fontWeight: "700", fontSize: "12px" }}>{CAT_ICONS[p.categorie]||"馃摝"} {p.nom}</div><div style={{ fontSize: "10px", color: "#888" }}>{p.code}</div><div style={{ fontSize: "13px", fontWeight: "900", color: C.vert }}>{fmt(p.prix)}</div></div>              <span style={{ backgroundColor: p.statut==="approuve" ? C.vertL : p.statut==="rejete" ? "#FFEBEE" : C.orL, color: p.statut==="approuve" ? C.vert : p.statut==="rejete" ? C.rouge : C.orange, borderRadius: "5px", padding: "2px 7px", fontSize: "9px", fontWeight: "700" }}>                {p.statut==="approuve" ? "鉁� En ligne" : p.statut==="rejete" ? "鉁� Rejet茅" : "鈴� En attente"}              </span>            </div>          </div>        ))}      </div>    </div>  );}function DashboardAcheteur({ user, setPage }) {  const [commandes, setCommandes] = useState([]);  useEffect(() => { db.from("commandes").select("*").eq("acheteur_tel", user?.telephone||"").then(({ data }) => setCommandes(data||[])); }, []);  return (    <div>      <div style={S.dashHead}><div style={{ fontSize: "17px", fontWeight: "900" }}>馃泹锔� Espace Acheteur</div><div style={{ fontSize: "10px", opacity: 0.8 }}>{user?.nom}</div></div>      <div style={S.wrap}>        <div style={S.statGrid}>          {[{ n: commandes.length, l: "Commandes" }, { n: commandes.filter(c=>c.statut==="livree").length, l: "Livr茅es" }].map((s,i) => (            <div key={i} style={S.statCard}><div style={{ fontSize: "18px", fontWeight: "900", color: C.vert }}>{s.n}</div><div style={{ fontSize: "10px", color: "#888" }}>{s.l}</div></div>          ))}        </div>        <button style={{ ...S.btn, maxWidth: "190px", marginBottom: "14px", fontSize: "12px" }} onClick={() => setPage("mes-commandes")}>馃摝 Mes commandes</button>        {commandes.slice(0, 3).map(c => {          const s = STATUTS.find(x=>x.key===c.statut)||STATUTS[0];          return <div key={c.id} style={S.row}><div style={{ display: "flex", justifyContent: "space-between" }}><div><div style={{ fontWeight: "700", fontSize: "12px" }}>{c.produit_nom}</div><div style={{ fontSize: "13px", fontWeight: "900", color: C.vert }}>{fmt(c.prix)}</div></div><span style={S.badge(s.color)}>{s.icon} {s.label}</span></div></div>;        })}      </div>    </div>  );}function PageMesCommandes({ user, setPage }) {  const [commandes, setCommandes] = useState([]);  const [sel, setSel] = useState(null);  useEffect(() => { db.from("commandes").select("*").eq("acheteur_tel", user?.telephone||"").then(({ data }) => setCommandes(data||[])); }, []);  if (sel) return (    <div style={{ maxWidth: "700px", margin: "0 auto", padding: "14px 10px" }}>      <button style={{ background: "none", border: "none", color: C.vert, fontWeight: "700", cursor: "pointer", fontSize: "13px", marginBottom: "12px", padding: 0 }} onClick={() => setSel(null)}>鈫� Mes commandes</button>      <div style={{ backgroundColor: C.blanc, borderRadius: "14px", padding: "18px", boxShadow: "0 2px 10px rgba(0,0,0,0.07)" }}>        <div style={{ fontSize: "15px", fontWeight: "900", color: C.vert, marginBottom: "10px" }}>馃摝 Suivi 鈥� {sel.code}</div>        {[["Produit",sel.produit_nom],["Montant",fmt(sel.prix)],["Livraison",`${sel.ville} 鈥� ${sel.adresse}`]].map(([k,v]) => (          <div key={k} style={{ display: "flex", justifyContent: "space-between", padding: "6px 0", borderBottom: "1px solid #F0F0F0", fontSize: "12px" }}>            <span style={{ color: "#888" }}>{k}</span><span style={{ fontWeight: "700" }}>{v}</span>          </div>        ))}        <div style={{ marginTop: "12px", position: "relative", paddingLeft: "22px" }}>          <div style={{ position: "absolute", left: "7px", top: 0, bottom: 0, width: "2px", backgroundColor: "#E0E0E0" }} />          {STATUTS.map((s, i) => {            const idx = STATUTS.findIndex(x => x.key === sel.statut);            const done = i <= idx;            return <div key={s.key} style={{ position: "relative", marginBottom: "14px" }}>              <div style={{ position: "absolute", left: "-22px", top: "2px", width: "14px", height: "14px", borderRadius: "50%", backgroundColor: done ? C.vert : "#E0E0E0", display: "flex", alignItems: "center", justifyContent: "center", fontSize: "7px", color: "#fff" }}>{done ? "鉁�" : ""}</div>              <div style={{ fontWeight: done ? "700" : "400", color: done ? C.vert : "#bbb", fontSize: "12px" }}>{s.icon} {s.label}</div>            </div>;          })}        </div>        <a href={`https://wa.me/224624168584?text=${encodeURIComponent(`Suivi commande ${sel.code} 鈥� ${sel.produit_nom}`)}`} target="_blank" rel="noreferrer" style={{ textDecoration: "none" }}>          <button style={{ ...S.waBtn, marginTop: "12px", padding: "10px" }}>馃摫 Contacter HALIMAD</button>        </a>      </div>    </div>  );  return (    <div style={S.wrap}>      <div style={{ fontSize: "17px", fontWeight: "900", color: C.vert, marginBottom: "3px" }}>馃洅 Mes commandes</div>      <div style={{ color: "#888", fontSize: "11px", marginBottom: "16px" }}>Donn茅es en temps r茅el</div>      {commandes.length === 0        ? <div style={{ textAlign: "center", padding: "36px", color: "#bbb" }}><div style={{ fontSize: "36px" }}>馃洅</div><div>Aucune commande</div><button style={{ ...S.navBtn(true), marginTop: "10px" }} onClick={() => setPage("accueil")}>Voir les produits 鈫�</button></div>        : commandes.map(c => { const s = STATUTS.find(x=>x.key===c.statut)||STATUTS[0]; return <div key={c.id} style={{ ...S.row, cursor: "pointer" }} onClick={() => setSel(c)}><div style={{ display: "flex", justifyContent: "space-between" }}><div><div style={{ fontWeight: "700", fontSize: "12px" }}>{c.produit_nom}</div><div style={{ fontSize: "10px", color: "#888" }}>{c.code}</div><div style={{ fontSize: "13px", fontWeight: "900", color: C.vert }}>{fmt(c.prix)}</div></div><span style={S.badge(s.color)}>{s.icon} {s.label}</span></div></div>; })      }    </div>  );}// 鈹€鈹€鈹€ App 鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€鈹€export default function App() {  const [page, setPage] = useState("accueil");  const [user, setUser] = useState(null);  const [selectedProduct, setSelectedProduct] = useState(null);  const [commandeModal, setCommandeModal] = useState(null);  const [commandes, setCommandes] = useState([]);  const [search, setSearch] = useState("");  const { toasts, add: addToast } = useToast();  const handleCommander = (produit) => {    if (!user) { setPage("connexion"); addToast("Connectez-vous pour commander", "info", "馃攼"); return; }    setCommandeModal(produit);  };  const handleShare = (produit) => {    const msg = encodeURIComponent(`馃泹锔� Regardez ce produit sur HALIMAD !\n\n${produit.nom}\n馃挵 ${fmt(produit.prix)}\n馃搷 ${produit.ville}\n馃彿锔� ${produit.code}\n\nCommandez sur WhatsApp : +224 624 16 85 84`);    window.open(`https://wa.me/?text=${msg}`, "_blank");    addToast("Lien de partage ouvert !", "success", "鈫�");  };  const handleRate = async (produitId, note) => {    addToast(`Note ${note}/5 enregistr茅e !`, "success", "猸�");  };  const render = () => {    switch (page) {      case "accueil": return <PageAccueil setPage={setPage} setSelectedProduct={setSelectedProduct} onCommander={handleCommander} onShare={handleShare} search={search} setSearch={setSearch} />;      case "produit": return <PageProduit product={selectedProduct} setPage={setPage} onCommander={handleCommander} onShare={handleShare} onRate={handleRate} />;      case "vendre": return <PageVendre setPage={setPage} user={user} addToast={addToast} />;      case "connexion": return <PageConnexion setPage={setPage} setUser={setUser} addToast={addToast} />;      case "inscription": return <PageInscription setPage={setPage} setUser={setUser} addToast={addToast} />;      case "a-propos": return <PageAPropos setPage={setPage} />;      case "mes-commandes": return <PageMesCommandes user={user} setPage={setPage} />;      case "admin": return <DashboardAdmin addToast={addToast} />;      case "vendeur": return <DashboardVendeur user={user} setPage={setPage} />;      case "acheteur": return <DashboardAcheteur user={user} setPage={setPage} />;      default: return <PageAccueil setPage={setPage} setSelectedProduct={setSelectedProduct} onCommander={handleCommander} onShare={handleShare} search={search} setSearch={setSearch} />;    }  };  return (    <div style={S.app}>      <Navbar setPage={setPage} user={user} setUser={setUser} panierCount={commandes.filter(c=>c.statut==="en_attente").length} search={search} setSearch={setSearch} />      {commandeModal && <ModalCommande produit={commandeModal} user={user} onClose={() => setCommandeModal(null)} onSuccess={(cmd) => { setCommandes(p => [cmd,...p]); setCommandeModal(null); }} addToast={addToast} />}      <main>{render()}</main>      <footer style={S.footer}>        <div style={{ color: C.or, fontWeight: "900", fontSize: "16px", marginBottom: "4px" }}>HALIMAD</div>        <div>Marketplace v茅rifi茅e 路 Madina 路 Conakry 路 Lab茅 路 Guin茅e</div>        <div style={{ marginTop: "4px" }}>馃摫 +224 624 16 85 84</div>        <div style={{ marginTop: "8px", display: "flex", justifyContent: "center", gap: "12px" }}>          <span style={{ cursor: "pointer", textDecoration: "underline" }} onClick={() => setPage("a-propos")}>脌 propos</span>          <span style={{ cursor: "pointer", textDecoration: "underline" }} onClick={() => setPage("vendre")}>Vendre</span>          <a href="https://wa.me/224624168584" target="_blank" rel="noreferrer" style={{ color: "rgba(255,255,255,0.75)", textDecoration: "underline" }}>Contact</a>        </div>        <div style={{ marginTop: "5px", fontSize: "9px", opacity: 0.4 }}>漏 2025 HALIMAD 路 Tous droits r茅serv茅s</div>      </footer>      <WAFloat />      <Toast toasts={toasts} />    </div> );}
